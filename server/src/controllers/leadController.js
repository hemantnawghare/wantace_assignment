export async function getLeadsController(req, res) {
  res.json({ message: 'Leads endpoint available.' });
}

export async function createLeadController(req, res) {
  res.json({ message: 'Lead creation endpoint available.' });
}
